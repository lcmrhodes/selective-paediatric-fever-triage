# Reproducibility settings —-

analysis:
  seed: 202504
  held_out_site: kh005
  bootstrap_replicates: 5000
  bootstrap_seed: 209504
  probability_tolerance: 1.0e-12
  reported_value_tolerance: 5.0e-4

# Fixed traffic-light thresholds —-

thresholds:
  green_upper_exclusive: 0.005
  amber_upper_inclusive: 0.02

# Local data and output locations —-

paths:
  raw: data/raw
  derived: data/derived
  outputs: outputs

# Authorized source filenames —-

source_files:
  follow_up: AllFields_Follow-up_Nov2025.csv
  discharge: AllFields_Discharge_Nov2025.csv
  enrolment: AllFields_Enrolment_Nov2025.csv
  analysis_fields: predictive.analysis.univariate_17Oct2024.xlsx
